<Window x:Class="UchetNZP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Учет НЗП — Desktop"
        Width="1440"
        Height="900"
        MinWidth="1024"
        MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="#0B1220">
    <Window.Resources>
        <SolidColorBrush x:Key="PanelBrush" Color="#111A2E" />
        <SolidColorBrush x:Key="PanelLightBrush" Color="#1A2742" />
        <SolidColorBrush x:Key="AccentBrush" Color="#32C7FF" />
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#1B2A46" />
            <Setter Property="BorderBrush" Value="#2A436F" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="12,8" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#24406A" />
                    <Setter Property="BorderBrush" Value="#3A6AA7" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.45" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="{StaticResource PanelBrush}" CornerRadius="14" Padding="14" Margin="0,0,0,10" BorderBrush="#22385E" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Grid Width="48" Height="48" Margin="0,0,12,0">
                    <Ellipse Fill="#16325A" Stroke="{StaticResource AccentBrush}" StrokeThickness="2" />
                    <Ellipse Width="34" Height="34" Stroke="#88FFFFFF" StrokeThickness="1" />
                    <TextBlock Text="NZP" Foreground="White" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="13" />
                </Grid>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="Учет НЗП" Foreground="White" FontSize="20" FontWeight="SemiBold" />
                    <TextBlock Text="Desktop Client" Foreground="#A7C3E8" FontSize="12" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="14" Padding="12" Margin="0,0,0,10" BorderBrush="#22385E" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button x:Name="BackButton" Style="{StaticResource NavButtonStyle}" Click="BackButton_OnClick">← Назад</Button>
                    <Button x:Name="ForwardButton" Style="{StaticResource NavButtonStyle}" Click="ForwardButton_OnClick">Вперед →</Button>
                    <Button x:Name="RefreshButton" Style="{StaticResource NavButtonStyle}" Click="RefreshButton_OnClick">⟳ Обновить</Button>
                    <Button x:Name="StopButton" Style="{StaticResource NavButtonStyle}" Click="StopButton_OnClick">✕ Стоп</Button>
                    <Button x:Name="HomeButton" Style="{StaticResource NavButtonStyle}" Click="HomeButton_OnClick">⌂ Домой</Button>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <TextBox x:Name="AddressBox" Width="480" Height="38" KeyDown="AddressBox_OnKeyDown" Background="#0F1B31" Foreground="White" BorderBrush="#2A436F" BorderThickness="1" Padding="10,8" VerticalContentAlignment="Center" />
                    <Button x:Name="GoButton" Style="{StaticResource NavButtonStyle}" Margin="8,0,0,0" Click="GoButton_OnClick">Перейти</Button>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" CornerRadius="14" BorderBrush="#22385E" BorderThickness="1" Background="{StaticResource PanelLightBrush}" ClipToBounds="True">
            <wv2:WebView2 x:Name="Browser" />
        </Border>

        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" CornerRadius="10" Padding="10" Margin="0,10,0,0" BorderBrush="#22385E" BorderThickness="1">
            <DockPanel>
                <Ellipse Width="10" Height="10" Fill="{StaticResource AccentBrush}" Margin="0,0,8,0" VerticalAlignment="Center" />
                <TextBlock x:Name="StatusText" Foreground="#D6E7FF" VerticalAlignment="Center">Запуск...</TextBlock>
            </DockPanel>
        </Border>
    </Grid>
</Window>
