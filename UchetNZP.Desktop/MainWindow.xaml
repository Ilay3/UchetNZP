<Window x:Class="UchetNZP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="Учет НЗП — Desktop"
        Width="1440"
        Height="900"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock x:Name="StatusText">Запуск...</TextBlock>
            </StatusBarItem>
        </StatusBar>

        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button x:Name="BackButton" Click="BackButton_OnClick">Назад</Button>
                <Button x:Name="ForwardButton" Click="ForwardButton_OnClick">Вперед</Button>
                <Button x:Name="RefreshButton" Click="RefreshButton_OnClick">Обновить</Button>
                <Button x:Name="StopButton" Click="StopButton_OnClick">Стоп</Button>
                <Button x:Name="HomeButton" Click="HomeButton_OnClick">Домой</Button>
                <Separator />
                <TextBox x:Name="AddressBox" Width="720" KeyDown="AddressBox_OnKeyDown" />
                <Button x:Name="GoButton" Click="GoButton_OnClick">Перейти</Button>
            </ToolBar>
        </ToolBarTray>

        <wv2:WebView2 x:Name="Browser" />
    </DockPanel>
</Window>
